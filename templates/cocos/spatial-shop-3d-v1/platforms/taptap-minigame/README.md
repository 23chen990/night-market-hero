# TapTap Mini Game target

This target is intentionally isolated and conditional. Install and verify TapTap's official `taptap-minigame-tools` 1.2.2 plugin in Cocos Creator 3.8.x before producing `build/TapBuild/game.zip`. A web build is not TapTap publishability evidence.
